# TSO resources

Single-file HTML resource assistant for Transitional Services offices.

Open `new.html` directly in a browser to use the app. It starts with a standard set of categories but no resources; press Ctrl+Alt+A, then click the Admin tab to add, edit, delete, import, export, and manage resource packages.
