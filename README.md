# Albuquerque TSO resources

Single-file HTML resource assistant based on the existing TSO app design.

Open `albq.html` directly in a browser to use the app. It starts with no categories and no resources; use the Admin tab to add, edit, delete, import, export, and manage resource packages.
